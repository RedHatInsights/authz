version: 1
threshold:
  compliance: 100
  coverage: 0
dependenciesRules:
  - package: "domain"
    shouldOnlyDependsOn:
      internal:
        - "domain.**"
    shouldNotDependsOn:
      external: [ "*.*" ]
