package interceptor

import (
	"testing"
)

func TestInterceptorHoldsValuesFromDiscoveryEndpoint(t *testing.T) {
	//interceptor := NewAuthnInterceptor(nil)

	//assert.Equal(t, "issuer", interceptor.provider.Issuer)
}
